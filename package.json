{
  "name": "vscode-integrated-browser-mcp",
  "displayName": "Integrated Browser MCP",
  "description": "Exposes VS Code's Integrated Browser as an MCP server so external agents (Claude Code, etc.) can control it",
  "version": "0.0.1",
  "publisher": "Nagell",
  "engines": {
    "vscode": "^1.112.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "mcp",
    "browser",
    "ai",
    "agent",
    "claude"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "type": "module",
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "integratedBrowserMcp.startServer",
        "title": "Integrated Browser MCP: Start Server"
      },
      {
        "command": "integratedBrowserMcp.stopServer",
        "title": "Integrated Browser MCP: Stop Server"
      },
      {
        "command": "integratedBrowserMcp.listTools",
        "title": "Integrated Browser MCP: List Available LM Tools (debug)"
      },
      {
        "command": "integratedBrowserMcp.testInvoke",
        "title": "Integrated Browser MCP: Test invokeTool (debug)"
      },
      {
        "command": "integratedBrowserMcp.probeSchemas",
        "title": "Integrated Browser MCP: Probe Tool Schemas (debug)"
      },
      {
        "command": "integratedBrowserMcp.testPageTools",
        "title": "Integrated Browser MCP: Test read_page + screenshot_page (debug)"
      }
    ],
    "configuration": {
      "title": "Integrated Browser MCP",
      "properties": {
        "integratedBrowserMcp.port": {
          "type": "number",
          "default": 3100,
          "description": "Port the MCP server listens on"
        },
        "integratedBrowserMcp.autoStart": {
          "type": "boolean",
          "default": true,
          "description": "Start the MCP server automatically when VS Code opens"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "pnpm run bundle",
    "compile": "tsc -p ./",
    "bundle": "node esbuild.mjs",
    "watch": "tsc -watch -p ./",
    "pretest": "pnpm run compile && pnpm run lint",
    "test": "vscode-test",
    "lint": "eslint src"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.112.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "esbuild": "^0.28.0",
    "eslint": "^9.39.3",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.56.1"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.11.0",
    "express": "^5.2.1",
    "zod": "^4.4.3"
  },
  "license": "MIT",
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
import type { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
import type { ToolContext } from './_context.js';

// Populated in U12 (console capture tools)
export function registerDiagnosticTools(_server: McpServer, _ctx: ToolContext): void {
}
